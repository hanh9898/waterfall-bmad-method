# Decision Log — hbc-create-test-plan (D-26)

## 2026-06-12 — Session: Create

### Sources
- D-02 (24 REQ), D-06 (flows), D-19 (ER), UX EXPERIENCE.md. Stack: Odoo 11 Python.

### Decisions
- **Intent:** Test Plan (strategy). D-26 trước, D-27 bám theo.
- **Test levels in-scope:** Unit (Odoo TransactionCase), Integration (generate→period/member, sync, lifecycle), System/E2E UI (grid, dialog, permission).
- **Performance:** KHÔNG tự động hóa — chỉ spot-check tay NFR-001/002 (user không chọn cấp Performance).
- **Framework:** Odoo test framework (unit+integration) + Playwright (E2E UI).
- **Coverage threshold:** 80% (config).
- **Risk seams (facet-split):** sync 1 chiều ghi vào project.member (dữ liệu dùng chung) = rủi ro cao; lock bypass; overwrite data loss; plan-level lifecycle REQ-024 (mới, chưa từng test).
