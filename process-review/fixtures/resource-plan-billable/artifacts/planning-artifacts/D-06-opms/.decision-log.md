---
skill: hbc-create-business-flow-diagram
project_name: opms
updated: 2026-06-11
---

# Decision Log — hbc-create-business-flow-diagram

## 2026-06-11T00:00 — Session: Create

### Sources
- `_bmad-output/planning-artifacts/D-02-resource-plan-billable.md` (D-02 Requirements, 23 REQ)
- `_bmad-output/planning-artifacts/D-03-resource-plan-billable-glossary.md` (D-03 Glossary)
- Discovery notes (phiên D-02): AS-IS dùng Google Sheet; TO-BE nhập trên OPMS.

### Mode and scope decisions
- **Mode:** migration — AS-IS (Google Sheet thủ công) → TO-BE (resource plan trên OPMS). `as_is.has_as_is=false` trong D-02 nhưng AS-IS đã rõ từ discovery; tiếp tục migration.
- **Scope:** multiple named flows — (1) AS-IS billable thủ công, (2) TO-BE nhập resource plan & sync allocation, (3) TO-BE Generate billable & xử lý tháng khóa.
- **Diagram type:** cả sequenceDiagram (tương tác tổng thể) + flowchart (luồng Generate nhiều nhánh) — người dùng chọn "cả hai".

### Discovery snapshot (Stage 2 flush)
- **Actors:** Planner (Invoice Manager / Department Manager / Delivery Manager), OPMS (hệ thống), Allocation store (`project.member`), Billable store (`project.invoice.period` + `project.invoice.member`), Sales (ngoài hệ thống). AS-IS thêm: Google Sheet.
- **Triggers:** (AS-IS) định kỳ hàng tháng tổng hợp tay; (TO-BE nhập) bộ phận cập nhật kế hoạch nguồn lực; (TO-BE generate) người dùng bấm Generate.
- **Flow inventory:**
  - AS-IS — Lập kế hoạch & tạo billable thủ công qua Google Sheet.
  - TO-BE Flow 1 — Nhập/sửa/xóa resource plan, đồng bộ một chiều sang allocation.
  - TO-BE Flow 2 — Generate billable từ resource plan, bỏ qua tháng khóa, xác nhận ghi đè.

### Validation findings
- Mermaid syntax: pass — 4 block (3 sequenceDiagram + 1 flowchart). Block flowchart chỉ có ghi chú "không kiểm cấu trúc tự động" (soi LLM, hợp lệ). `render_check: skipped` (mmdc chưa cài) — render chưa verify, kiểm cấu trúc + soi tay.
- Actor coverage: pass — expected actors đều xuất hiện.
- FR mapping: pass — 23/23 REQ covered, 0 phantom (sau khi thay viết tắt slash-form bằng ID đầy đủ cho REQ-005/008/009).
- Auto-fixes applied: none (interactive).

### Handoff target
- `hbc-create-er-diagram` (D-19, model resource plan + quan hệ với period/member/allocation), `hbc-create-test-plan` (D-26).

## 2026-06-11T00:30 — Session close (Create)
- Sources: D-02, D-03. Mode: migration. Scope: 3 luồng (AS-IS + 2 TO-BE). Diagram: sequenceDiagram + flowchart.
- Lenses run: none (skip).
- Distillator: bmad-distillator không có trong danh sách skill khả dụng → bỏ qua (không thay thế).
- D-06 lastStep = complete.
