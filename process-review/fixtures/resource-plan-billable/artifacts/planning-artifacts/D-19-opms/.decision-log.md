---
skill: hbc-create-er-diagram
project_name: opms
updated: 2026-06-12
---

# Decision Log — hbc-create-er-diagram (D-19)

## 2026-06-12 — Session: Create

### Sources
- `_bmad-output/planning-artifacts/D-02-resource-plan-billable.md` (D-02, 24 REQ)
- `_bmad-output/planning-artifacts/ux-designs/ux-opms-2026-06-11/EXPERIENCE.md` (UX — entities & states)
- `_bmad-output/planning-artifacts/D-06-opms/D-06-business-flow-diagram.md` (D-06 flows)
- Kiến thức model Odoo hiện có (khảo sát codebase `project_invoice`, `ntq_project`).

### Mode and scope decisions
- **Intent:** Both — thiết kế model MỚI (resource plan) + thể hiện quan hệ tới schema HIỆN CÓ.
- **Scope:** single-domain (resource plan → billable), tập trung entity của feature; entity hiện có chỉ vẽ ở mức liên quan (không tái tài liệu toàn schema OPMS).
- **Normalization level:** logical (entity + key attributes + relationships); physical name snake_case kiểu bảng Odoo.
- **Nguồn auto-extract nhiễu** (tách từ tiếng Việt) → dùng LLM extraction.

### Discovery snapshot (Stage 2 flush)
- **Entities mới:** resource_plan, resource_plan_line, resource_plan_line_month.
- **Entities hiện có (liên quan):** project_project, hr_employee, hr_department, ntq_project_role_skills, ntq_project_billable_rate, project_member, project_invoice_period, project_invoice_member.
- **Quyết định mô hình:**
  - MM theo tháng → **bảng con resource_plan_line_month** (line_id, month_date, effort_mm) — hợp khoảng tháng động.
  - Link sync → **member_id FK** trên resource_plan_line trỏ project_member (hiện thực REQ-011/012/013; xóa = set end_at chứ không xóa cứng).
- **Quan hệ:** plan ─<line ─<line_month; line─employee/dept/role/rate; line─member(1-1, non-cứng); plan ··generates·· period ─<invoice_member (Generate không tạo FK plan→period; period keyed theo project+month).
- **Giả định:** unique(project_id) trên resource_plan (1 plan/dự án — theo D-02). unique(plan_id, employee_id) trên line; unique(line_id, month_date) trên line_month.

### Validation findings (Stage 4)
- **validate-mermaid-er.py:** 1 ER block parse OK. 10 issue `fk_target_missing` = **false-positive**: heuristic singularize tên cột FK (project_id→"Project") không khớp tên bảng Odoo snake_case (project_project). Đã xác nhận từng FK bị cờ đều có relationship line tới entity đã khai báo → không sửa. Đã trim các orphan FK thật (submitted_by/approved_by→res_users, hr_employee.department_id, rate.currency_id, project_member nội bộ, invoice_member.employee_id/rate_id) khỏi sơ đồ; documented FK ở table def.
- **check-entity-coverage.py:** vô nghĩa — bộ trích entity phía D-02 (tiếng Việt) trả stopword ["các","cả","một"], coi 11 entity thật là phantom. Fallback LLM judgment.
- **LLM judgment:** normalization 3NF hợp lý; relationship-completeness pass (0 orphan sau trim); naming snake_case nhất quán; revision history đầy đủ; ngôn ngữ nhất quán. PASS.

### Session close (Create)
- Sources: D-02, UX EXPERIENCE.md, D-06, codebase Odoo. Scope: single-domain. Level: logical.
- 3 entity mới + 8 entity hiện có (liên quan). Lenses run: none.
- Handoff: hbc-create-api-spec (D-21), hbc-create-coding-standards (D-12), hbc-task-breakdown.
- Distillate: on_complete_distillate=offer → đề nghị user (bmad-distillator nếu có).
- D-19 lastStep = complete.
