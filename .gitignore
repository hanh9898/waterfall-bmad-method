node_modules/
.DS_Store
__pycache__/
.pytest_cache/

# BMad personal config (per-user, not shared)
_bmad/config.user.yaml

# Agent isolation worktrees (transient, never commit)
.claude/worktrees/

# Skill analysis output + builder memlog (ephemeral, per-run)
src/**/.analysis/
src/**/.memlog.md
