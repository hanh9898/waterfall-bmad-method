# DO NOT EDIT -- overwritten on every update.
#
# Workflow customization surface for hbc-create-requirements.
# Team overrides:     {project-root}/_bmad/custom/hbc-create-requirements.toml
# Personal overrides: {project-root}/_bmad/custom/hbc-create-requirements.user.toml

[workflow]

# --- Configurable below. Overrides merge per BMad structural rules: ---
#   scalars: override wins • arrays (persistent_facts, activation_steps_*): append

activation_steps_prepend = []

activation_steps_append = []

persistent_facts = [
  "file:{project-root}/**/project-context.md",
]

# D-02 template path. Override to use a custom template with different sections.
template_path = "assets/D-02_requirements_template.md"

# Output path for the generated D-02 document — PER-FEATURE ({feature} resolved at runtime).
output_dir = "{output_folder}/features/{feature}/planning-artifacts"

# Validation script. Override to use a custom validator with the same CLI interface.
validation_script = "scripts/validate-requirements.py"

# Scan script for source discovery (state, sources, project-context, brownfield catalog).
scan_script = "scripts/scan-sources.py"

# Vague terms to flag during validation (source of truth; the resolver joins this
# array with commas for --vague-terms). Override for your domain; arrays append-merge.
vague_terms = ["fast", "easy", "user-friendly", "simple", "good", "nice", "efficient", "appropriate", "adequate", "reasonable"]

# NOTE: the brownfield change-type vocabulary (NEW / CHANGE / REMOVE) is intentionally
# NOT customizable — it is a fixed controlled vocabulary (like EARS keywords) keyed on
# by the template, validator, gate and rubric. Changing it means forking those together.

# hbc-traceability impact integration (REQ-005, BR-13). When true, after a successful `update`
# auto-invoke hbc-traceability impact to cascade downstream. Default false (no behavior change).
# Skills invoked WITH --invoked-by-sync must suppress this. See hbc-traceability/references/impact-capability.md
auto_sync_after_update = false
