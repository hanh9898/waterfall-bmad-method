---
phase: build
classification: workflow-skill
last_touched: 2026-05-27
---

# Decision Log — hbc-create-requirements

## 2026-05-27 — Initial Build

**Context:** Wave 2, first Phase 1 Analysis skill. Generates D-02 Requirements Specification with REQ-xxx IDs that form the traceability foundation.

**Decisions:**

1. **5-stage workflow** — Prerequisites → Discovery → Generation → Validation → Save. Follows established HBC pattern from hbc-create-business-flow-diagram.

2. **Deterministic validation script** — `validate-requirements.py` checks REQ ID uniqueness/sequencing, vague terms, required sections, and NFR measurability. Returns JSON with `auto_fixable` flags per issue.

3. **Vague terms configurable** — Default list in customize.toml, overridable via team/personal TOML layers. Script also accepts `--vague-terms` CLI override.

4. **Bilingual template (VI source / EN)** — D-02 template + validator are Vietnamese/English (English canonical + configured-language label; no hardcoded Japanese). (Corrected: an earlier entry mislabeled this as Japanese.)

5. **Headless mode** — Sources required as args, returns JSON per headless-contract.md. Auto-fixes applied, `blocked` on manual-fix issues.

6. **REQ-xxx as traceability anchor** — Sequential IDs starting from REQ-001. These propagate to traceability matrix (hbc-traceability) and are the Phase 1 gate requirement.

## 2026-05-27 — Quality analysis (1st run)

Grade: B+. Interactive HTML: `.analysis/2026-05-27T103000/quality-report.html`. Zero broken items. 16 opportunities (3 high, 10 medium, 3 low). Key themes: headless input contract missing, vague_terms merge chain bypass, facilitative patterns.

## 2026-05-27 — Quality analysis (2nd run)

Grade: A- → A after fixes. 2 broken items fixed: B1 output_dir wired via {workflow.output_dir} in Stages 3-4, B2 scan-sources.py now has 9 tests. 11 opportunities fixed: O1 decision-log to skill root, O2 headless collapsed, O3 auto_fixes in return, O4 sibling skills named, O5 mode_conflict reason, O6 vague_terms serialization doc, O7 template placeholder comment, O8 decision-log finalize audit, O9 subagent fallback, O10 bilingual section matching in validator, O11 sys.exit(0) in scan-sources.py. Test files renamed to underscore convention. Total: 20/20 tests pass. Report: `.analysis/2026-05-27T193000/report-data.json`.
