# Architecture Analysis — hbc-create-business-flow-diagram (Round 7, post round-6 polish)

Scanner: L1 (architecture) · Date: 2026-05-14 · Round-6 delta of round 5 (~237 → 258 SKILL.md lines)

## Assessment

Round 6 closed every round-5 finding and the skill remains firmly **Excellent**. The ~22-line growth is load-bearing — every new sentence either lifts a real architectural decision out of prose (`--scope-of-change` gate, `fresh_reason` discriminator, zero-actor branch, migration-vs-AS-IS sanity check, U1/U2 update sub-options) or hardens the headless contract surface (closed-set `reason` mirror in SKILL.md). The size envelope (258 lines / 4,690 tokens, principles file's "multi-branch SKILL.md: up to ~250 lines" — *3% over the soft target*, well under the ~500-line single-purpose hard ceiling). No new structural issues, no introduced contradictions, no execution-breaking defects. Two genuinely minor observations on the new surface, both low-severity.

## Round-5 findings: resolution audit

| Round-5 finding | Status | Evidence |
|---|---|---|
| N1 (high): scope-of-change gate for Update | **Resolved** | `SKILL.md:202-209` adds the explicit gate with three modes (`polish` / `semantic` / `auto`); `auto` is defined as "compare current `stage_2_actors` and `stage_2_flows` against the prior session's flush block in `.decision-log.md`. If both arrays are identical → polish; if either differs → semantic." Headless flag `--scope-of-change=polish\|semantic\|auto` (default `auto`) at `references/headless-contract.md:16` + defaults table row at `:30`. |
| N2 (medium): Stage 1b short-circuit tightened | **Resolved** | `SKILL.md:132` — "Only skip 1e when the open-floor reply *explicitly* covers **all four** of: mode, scope, sources (which docs to include or exclude), and diagram type. A partial reply (any of the four absent or ambiguous) falls through to 1e for confirmation." Plus the obligation to log which dimensions were inferred-vs-confirmed. The strict-AND wording closes the prior "any signal triggers skip" ambiguity. |
| N3 (medium): `fresh_reason` discriminator | **Resolved** | `discover-planning-artifacts.py:181-194` adds three values: `no_workspace` (no primary), `crashed_no_progress` (primary exists, `stepsCompleted` empty), `stale_artifact` (frontmatter exists but no `stage-1` yet). `SKILL.md:97-100` consumes them; menu line `:107` echoes back the value. The third value (`stale_artifact`) is not in the round-6 delta description but is a sound completeness move — covers the awkward intermediate state where a primary exists with frontmatter but never reached stage-1 write. |
| M-1 (medium): Overview "between PRD and Architecture" → Stage 5 enumerator | **Resolved** | `SKILL.md:12` now reads "Stage 5 enumerates the canonical downstream consumers." One source of truth. The Stage 5 list at `:250` is the authoritative enumeration. |
| M-2 (medium): closed-set `reason` values mirrored in SKILL.md | **Resolved** | `SKILL.md:55-67` now carries the full closed set of seven reasons (the original five plus `migration_without_as_is` and `resolver_missing`). Final line `:67` — "Add new reasons only by extending this list and the contract file together" — names the drift-control requirement explicitly. Headless-contract.md's table at `:63-73` mirrors it. The full-spec table (when each fires + automator language) stays in the contract file. |
| N-1 (low): meta-explanation in headless-contract.md preamble | **Resolved** | `references/headless-contract.md:3` no longer carries the "Carved out per principle…" sentence. The opening now reads as content ("Authoritative reference for `--headless` / `-H` invocation… the brief reason list in SKILL.md is a compaction-survival mirror, not a separate source of truth"). That second clause is integrity language tied to M-2's mirror decision, not meta about the carve-out decision — it earns its place. |
| N-3 (low): Parallel-lens menu heading | **Resolved** | `SKILL.md:69` reads `## Parallel-lens menu`; the parenthetical lives in the section body at `:71` where it can be edited without churning the TOC. |
| N-4 (low): stale "If FR coverage was not requested" sentence | **Resolved** | `SKILL.md:225` now reads "If the PRD has no FR identifiers, `uncovered` and `phantom` will both be empty." Aligns with `check-fr-coverage.py` having `--prd required=True, action="append"`. |
| N-2 (low): stale `__pycache__` + missing `.gitignore` | **Resolved** | `.gitignore` present at skill root with the standard Python set (`__pycache__/`, `*.py[cod]`, `*$py.class`, `.scan/`, `*.egg-info/`, `.pytest_cache/`, `.coverage`). Stale `__pycache__/` is no longer in `scripts/tests/`. |
| N5 (low): decision-log Session heading regex language-agnostic | **Resolved** | `discover-planning-artifacts.py:158` — `r"^##\s+([^\n]*\d{4}-\d{2}-\d{2}[^\n]*)$"`. Inline comment at `:154-155` calls out the design rationale (matches English "## 2026-05-14T10:23 — Session: Update", Vietnamese "## 2026-05-14T10:23 — Phiên: Cập nhật", or any translated form). Date-anchored, not keyword-anchored. |
| SO1 (carryover): Stage 2 zero-actor branch | **Resolved** | `SKILL.md:184` — "if the source contains no human or system actors (e.g. pure data-pipeline products triggered solely by cron or queue events), do not render a vacuous 'System talks to itself' diagram. Promote the trigger (scheduled job, queue, webhook) to a first-class actor and explain the choice in the decision log." Both interactive (confirm with user) and headless (log and continue) branches specified. |
| SO2 (carryover): Stage 1e migration-vs-AS-IS sanity check | **Resolved** | `SKILL.md:169` — fires at confirmation: "if mode resolves to `migration` (either auto-inferred or via `--mode=migration`) but no PRD source contains AS-IS / 現状 / 'current state' / similar markers, warn the user and offer to switch to `greenfield`. In headless mode this returns `blocked` with `reason: 'migration_without_as_is'` unless the user passes `--allow-migration-without-as-is`". New `reason` value in the closed set (`SKILL.md:64`, `headless-contract.md:72`). New flag in the input table (`headless-contract.md:18`). Coherent end-to-end. |
| SO3 (carryover): Stage 1a U1/U2 Update sub-options + `--update-flow` | **Resolved** | `SKILL.md:113-114` interactive menu: `[U1] Update all flows` / `[U2] Update a single named flow (specify name)`. Headless flag at `:120` and `references/headless-contract.md:17`. Stage 3 honours the flag at `SKILL.md:200`: "Update mode with `--update-flow=<name>` (or interactive U2): re-render only the specified flow, leaving other Mermaid blocks untouched." Defaults table row at `:31`. Flow is consistent across all four surfaces. |
| L3 (carryover): word-boundary `\bprd\b` regex | **Resolved** | `discover-planning-artifacts.py:241` — `r"\bprd\b"` compiled case-insensitive. Inline comment explains: "matches `prd/` and `prd-customer/` but not `approved/` or `notes-on-the-prd/`." Correct word boundary semantics. |

**Round-5 → Round-6: 13 of 13 findings closed.** No carryovers remain.

Carryover from earlier rounds (not actionable):

- Prepass critical "Referenced stage file does not exist: 06-business-flow-diagram.md" (`workflow-integrity-prepass.json:120-125`). Same false positive from rounds 2/3/4/5: the prepass extracts `D-06-business-flow-diagram.md` (the OUTPUT document filename, appearing in five places across SKILL.md and the headless contract) as a stage reference. The skill has no carved stage files — the workflow is fully inline. The prepass regex is what wants tightening; the skill is correct.

## New issues introduced by round-6 polish

### Low

**N-1 — SKILL.md is 8 lines over the principles-file's "multi-branch up to ~250 lines" soft guidance**
`prompt-metrics-prepass.json:9` reports 259 lines / 4,690 tokens. The principles file (line 142) calls "multi-branch SKILL.md: up to ~250 lines" a target, not a hard limit, and explicitly invokes the "what fails if I delete this?" test for any individual line. The 22-line round-6 growth all passes that test (every new section is a real surface contract, not procedural padding). The skill is over the soft ceiling by ~3%, and well under the ~500-line single-purpose hard ceiling at line 143. No action required, but the skill is now near the natural carve-out threshold — *if a future round needs to add another stage-level surface*, the next carve candidate is the seven-row closed-set reason list at `SKILL.md:55-67` (it's table-shaped contract material and would naturally pair with the carved contract file). For now, the in-SKILL mirror is the right call because of M-2's compaction-survival concern.

**N-2 — `fresh_reason: "stale_artifact"` is implemented in the script but not surfaced in SKILL.md prose**
`discover-planning-artifacts.py:193-194` defines a third `fresh_reason` value (`stale_artifact`) for the case where primary frontmatter exists but `stepsCompleted` does not yet contain `stage-1`. SKILL.md (`:97-100`) enumerates only `no_workspace` and `crashed_no_progress` and does not name the third state. In practice the third state is rare (primary written but stage-1 not flushed — a different crash window from `crashed_no_progress`) and the menu line at `:107` would render it correctly because it just echoes whatever value the script produced. But an LLM reading the SKILL.md prose and seeing only two enumerated values may treat the third as undefined and ad-hoc the behaviour. Tighten by either:
- Adding a third bullet at `SKILL.md:97-100`: "`Fresh` with `fresh_reason: 'stale_artifact'` — primary frontmatter exists but `stepsCompleted` is empty *and* never reached stage-1 (rarer crash window). Treat the same as `crashed_no_progress`."
- Or collapse `stale_artifact` and `crashed_no_progress` in the script — the distinction is real but the runtime behaviour is the same.

Either is fine. This is the only place in the round-6 delta where the script grew a surface the prose didn't pick up.

## Specific-things-to-assess answers

**Is the skill now firmly Excellent?** Yes. All 13 round-5 findings closed. The two new low-severity findings above are minor — N-1 is "you are 8 lines over a soft target," N-2 is a 1-line documentation tightening for an edge state. Neither breaks execution, neither violates a stated promise, neither obscures intent.

**Any new structural issues introduced by round 6?** No structural issues. The 22-line growth distributes across five locations (1a menu adds 2 lines for U1/U2, scope-of-change gate adds 8 lines in Stage 3, zero-actor branch adds 3 lines in Stage 2, migration sanity check adds 2 lines in Stage 1e, reason mirror in SKILL.md adds 8 lines), each at the load-bearing point. No layering changes, no carve-out churn, no dependency-graph perturbation.

**Reason-values mirror in SKILL.md vs full table in headless-contract.md.** The duplication is acceptable and correct per the compaction-survival principle (principles-file lines 124, 226: "Carved-out files survive context compaction — critical instructions in the file itself"). The shape of the duplication is good: SKILL.md carries seven `reason: short-line` entries (~12 lines); headless-contract.md carries the same seven names with longer "when it fires" descriptions for automators. The drift risk is real and acknowledged: `SKILL.md:67` says "Add new reasons only by extending this list and the contract file together" — that's prose enforcement of the dual-write requirement. A future enhancement (out of scope for this regrade) would be a script-checked invariant ("the seven names in SKILL.md must equal the seven names in the contract"), but adding that check is gold-plating relative to the current state. The drift surface is small and the dual-write rule is named in-prose.

**Stage 3 scope-of-change gate: prose-defined diff procedure acceptable or lift to a script?** Acceptable as prose. The diff is two list comparisons — `current.stage_2_actors == prior.stage_2_actors` and `current.stage_2_flows == prior.stage_2_flows`. Per principles-file lines 113-115 ("Intelligence placement"), pure structural equality on two arrays the LLM has already loaded into context is exactly what the prompt should do — there's no parsing, no judgment, no fragile regex, no "what does this MEAN" decision. Lifting it to a script would add a round-trip script invocation (read `.decision-log.md` → parse the flush block → compare arrays → emit JSON → consume JSON) for a comparison the LLM can do in one step. The validators (`validate-mermaid.py`, `check-fr-coverage.py`) earn their script-side existence because they apply structural rules over Mermaid grammar and FR-identifier regex matching at scale — that's judgment-replacement-with-determinism. The Stage 2 flush diff is a one-liner the LLM can be trusted with. Keep as prose.

**`fresh_reason` discriminator: clean addition or surface creep?** Clean addition. The three values are mutually exclusive states that previously collapsed into "Fresh" — the audit trail genuinely needed the distinction (an automator hitting `crashed_no_progress` may want different recovery from `no_workspace`). The discriminator lives in the script (correct intelligence placement per principles 113-115) and is consumed transparently by the prompt. The only nit is N-2 above — the third state isn't surfaced in prose. The discriminator itself is well-shaped.

## Strengths to preserve (round-6 additions worth keeping)

- **`SKILL.md:67` dual-write requirement.** "Add new reasons only by extending this list and the contract file together" — explicit prose enforcement of the SKILL/contract mirror invariant from M-2. A future scanner could lint this, but the prose anchor is the thing that prevents drift in the meantime.
- **Stage 3 scope-of-change `auto` default.** The auto-diff using the Stage 2 flush block is one of the better round-6 design moves. It re-uses an artifact the workflow already produces (the compaction-flush at `SKILL.md:186-191`) rather than asking the LLM to re-derive scope from intuition. The fallback to manual interactive prompt and the explicit `--scope-of-change=polish|semantic|auto` headless flag give the same surface three coherent modes.
- **Zero-actor branch (Stage 2).** `SKILL.md:184` names a real failure mode (data-pipeline products triggered solely by cron/queue/webhook produce vacuous diagrams) and the correct fix (promote the trigger to a first-class actor). The branch fires at the right point (Stage 2 discovery, before Stage 3 renders Mermaid) and the decision is logged. Genuine domain framing per principles-file line 18.
- **Migration-vs-AS-IS sanity check at Stage 1e (`SKILL.md:169`).** Catches the mode/source mismatch *at the confirmation gate* — earliest place where both mode and sources are known. The `--allow-migration-without-as-is` escape hatch + new `migration_without_as_is` blocker `reason` make headless behaviour deterministic. The trio (warn / blocker / acknowledge) is the right shape.
- **U1/U2 sub-options + `--update-flow=<name>`.** `SKILL.md:113-114` (interactive menu), `:120` (headless mapping), `:200` (Stage 3 honours), `references/headless-contract.md:17` (flag table), `:31` (defaults table). Four-surface consistency for a single feature, no contradictions. The "Update single flow" mode is a real workflow primitive (the common case for revisable diagrams) that previously had no first-class surface.
- **Word-boundary `\bprd\b` regex in `discover-planning-artifacts.py:241`.** Surgical fix to L3 with an inline comment explaining the matcher's intent. The comment is the kind of design-rationale anchor principles-file line 19 wants.
- **Language-agnostic Session heading regex.** `discover-planning-artifacts.py:158` — `r"^##\s+([^\n]*\d{4}-\d{2}-\d{2}[^\n]*)$"`. Date-anchored matching means the script works whether the decision log is in English ("Session: Update"), Vietnamese ("Phiên: Cập nhật"), or any other `{document_output_language}` the workflow translates the template into. Comment at `:154-155` documents the intent. Tied directly to the Language Rules at `SKILL.md:22-27`.

(Round-4 → round-5 → round-6 carryover strengths — Decision-Log Workspace pattern, compaction-flush at Stage 2, intelligence-placement split between scripts and prompts, closed-set blocker enum, parallel-lens menu factoring, wrong-skill off-ramp, headless contract carve-out — all preserved.)

## Verdict

**Excellent.** Round 6 absorbed all 13 round-5 findings without introducing structural regressions. The 22-line growth in SKILL.md is fully load-bearing — every added sentence anchors a real architectural surface (gate / discriminator / branch / sanity check / closed-set mirror), not procedural padding. Two low-severity observations remain (3% over the soft size target, one undocumented `fresh_reason` value); both are tightening opportunities, not defects. The skill comfortably clears the principles-file bar on every load-bearing dimension: intelligence placement, decision-log workspace, headless contract surface, compaction survival, conventions stamp, description discipline, trigger-phrase coverage, intent-before-ingestion, capture-don't-interrupt, soft-gate elicitation, and parallel-review lenses. No finding in this round threatens execution or violates a stated promise.
