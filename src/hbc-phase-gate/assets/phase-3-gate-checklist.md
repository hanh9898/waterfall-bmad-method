# Phase 3: Implementation — Gate Checklist

> Per-feature gate: `{feature}` resolved at runtime. D-12 (Coding Standards) is **shared** → lives at `shared/`.

| item_id | description | type | required | artifact_pattern | criteria | skill_to_create |
|---------|-------------|------|----------|------------------|----------|-----------------|
| P3-00 | Phase 1 and Phase 2 gates PASSED (this feature) | CONTENT | yes | {output_folder}/features/{feature}/gates/phase-1-gate*,{output_folder}/features/{feature}/gates/phase-2-gate* | Status:.*PASSED | |
| P3-00b | Inter-document readiness still holds (re-assert IR seam, B3) | QUALITY | yes | {output_folder}/features/{feature}/planning-artifacts/D-02-*,{output_folder}/features/{feature}/planning-artifacts/D-27-* | Re-run `hbc-check-implementation-readiness` ([IR]) — `ready: true`. D-02/D-27 may have changed since the Phase 2 gate passed, so a stale `phase-2-gate PASSED` is not sufficient; the readiness reconciliation must still hold at Phase 3 entry. | hbc-check-implementation-readiness |
| P3-01 | Task breakdown document exists | FILE | yes | {output_folder}/features/{feature}/implementation-artifacts/task-breakdown* | | hbc-task-breakdown |
| P3-02 | All tasks completed | CONTENT | yes | {output_folder}/features/{feature}/implementation-artifacts/task-breakdown* | No rows with status TODO or IN_PROGRESS remaining. All tasks should show DONE. | |
| P3-02b | Implementation reality reconciled (D2) | QUALITY | yes | {output_folder}/features/{feature}/implementation-artifacts/task-breakdown*,{output_folder}/features/{feature}/traceability/matrix* | Run `validate-implementation.py --tasks <task-breakdown> --matrix <matrix> --tdd-evidence-dir {output_folder}/features/{feature}/implementation-artifacts/tdd-evidence --project-root <root>` (hbc-implement) — must be clean: no `DONE_TASK_NO_TEST` (DONE task no test assigned), `MISSING_CODE_FILE` (code_ref non-existent file), `REQ_NOT_IMPLEMENTED` (designed + tested but no code). | hbc-implement |
| P3-02c | TDD test-first: every DONE task has RED-evidence (cluster 1=C / 3) | QUALITY | yes | {output_folder}/features/{feature}/implementation-artifacts/tdd-evidence/* | Same `validate-implementation.py --tdd-evidence-dir <dir>` command as P3-02b — must be clean: no `NO_RED_EVIDENCE` (DONE task missing a RED-evidence file), no `RED_EVIDENCE_NO_FAIL` (evidence has no sign of the test FAILING before code). **Note: evidence is self-attested — a test-first discipline, not tamper-proof via git/timestamps.** | hbc-implement |
| P3-03 | Test coverage meets threshold | METRIC | yes | {output_folder}/features/{feature}/implementation-artifacts/coverage-report* | Coverage percentage >= {coverage_threshold}%. Extract the overall coverage number and compare. If no coverage artifact exists, guide the user: run `pytest --cov=. --cov-report=html:{output_folder}/features/{feature}/implementation-artifacts/coverage-report` (or framework equivalent) and re-run gate. | |
| P3-04 | All unit tests passing | QUALITY | yes | {output_folder}/features/{feature}/implementation-artifacts/test-results* | Test results show no failures. If no test results artifact exists, guide the user: run `pytest --tb=short > {output_folder}/features/{feature}/implementation-artifacts/test-results.txt` (or framework equivalent) and re-run gate. | |
| P3-05 | Code follows D-12 coding standards (shared) | QUALITY | no | {output_folder}/shared/coding-standards/D-12-* | Spot-check implementation code against D-12 standards. Flag obvious violations in naming, formatting, or patterns. | |
| P3-06 | E2E test scripts exist (if D-26 specifies E2E) | FILE | no | {output_folder}/features/{feature}/implementation-artifacts/e2e-* | | |
| P3-07 | Traceability matrix has code_ref for every implemented REQ (incremental, E) | QUALITY | yes | {output_folder}/features/{feature}/traceability/matrix* | Run `hbc-traceability` ([TRR]). Every REQ whose task is DONE must have a non-empty `code_ref` pointing at real source. Empty `code_ref` on a DONE task = code not linked back; populate via [TRU] now, don't defer to Phase 4. | hbc-traceability |
| P3-07s | Matrix completeness — every D-02 REQ has design+code+test_ref (A9, script-computed) [correctness] | MATRIX | yes | {output_folder}/features/{feature}/traceability/matrix* | d02={output_folder}/features/{feature}/planning-artifacts/D-02-* cols=design_ref,code_ref,test_ref — every D-02 REQ has a matrix row with all three refs non-empty. Numbers computed BY SCRIPT (B6-2). CORRECTNESS: never waived, never lenient-downgraded. | hbc-traceability |
