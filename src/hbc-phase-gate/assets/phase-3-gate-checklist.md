# Phase 3: Implementation — Gate Checklist

| item_id | description | type | required | artifact_pattern | criteria | skill_to_create |
|---------|-------------|------|----------|------------------|----------|-----------------|
| P3-00 | Phase 1 and Phase 2 gates PASSED | CONTENT | yes | {output_folder}/gates/phase-1-gate*,{output_folder}/gates/phase-2-gate* | Status:.*PASSED | |
| P3-00b | Inter-document readiness still holds (re-assert IR seam, B3) | QUALITY | yes | {output_folder}/planning-artifacts/D-02-*,{output_folder}/planning-artifacts/D-27-* | Re-run `hbc-check-implementation-readiness` ([IR]) — `ready: true`. D-02/D-27 may have changed since the Phase 2 gate passed, so a stale `phase-2-gate PASSED` is not sufficient; the readiness reconciliation must still hold at Phase 3 entry. | hbc-check-implementation-readiness |
| P3-01 | Task breakdown document exists | FILE | yes | {output_folder}/implementation-artifacts/task-breakdown* | | hbc-task-breakdown |
| P3-02 | All tasks completed | CONTENT | yes | {output_folder}/implementation-artifacts/task-breakdown* | No rows with status TODO or IN_PROGRESS remaining. All tasks should show DONE. | |
| P3-02b | Implementation reality reconciled (D2) | QUALITY | yes | {output_folder}/implementation-artifacts/task-breakdown*,{output_folder}/traceability/matrix* | Run `validate-implementation.py --tasks <task-breakdown> --matrix <matrix> --project-root <root>` (hbc-implement) — must be clean: no `DONE_TASK_NO_TEST` (a DONE task with no test assigned), no `MISSING_CODE_FILE` (matrix code_ref pointing at a non-existent file), no `REQ_NOT_IMPLEMENTED` (designed + tested but no code). Catches tasks closed with nothing behind them. | hbc-implement |
| P3-03 | Test coverage meets threshold | METRIC | yes | {output_folder}/implementation-artifacts/coverage-report* | Coverage percentage >= {coverage_threshold}%. Extract the overall coverage number and compare. If no coverage artifact exists, guide the user: run `pytest --cov=. --cov-report=html:{output_folder}/implementation-artifacts/coverage-report` (or framework equivalent) and re-run gate. | |
| P3-04 | All unit tests passing | QUALITY | yes | {output_folder}/implementation-artifacts/test-results* | Test results show no failures. If no test results artifact exists, guide the user: run `pytest --tb=short > {output_folder}/implementation-artifacts/test-results.txt` (or framework equivalent) and re-run gate. | |
| P3-05 | Code follows D-12 coding standards | QUALITY | no | {output_folder}/planning-artifacts/D-12-* | Spot-check implementation code against D-12 standards. Flag obvious violations in naming, formatting, or patterns. | |
| P3-06 | E2E test scripts exist (if D-26 specifies E2E) | FILE | no | {output_folder}/implementation-artifacts/e2e-* | | |
| P3-07 | Traceability matrix has code_ref for every implemented REQ (incremental, E) | QUALITY | yes | {output_folder}/traceability/matrix* | Run `hbc-traceability` ([TRR]). Every REQ whose task is DONE must have a non-empty `code_ref` pointing at real source. Empty `code_ref` on a DONE task = code not linked back; populate via [TRU] now, don't defer to Phase 4. | hbc-traceability |
