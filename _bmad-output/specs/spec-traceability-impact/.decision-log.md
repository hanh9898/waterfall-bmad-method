# Decision Log — spec-traceability-impact

## 2026-06-17 — Create (từ blueprint brainstorming)

**Nguồn:** `_bmad-output/brainstorming/brainstorming-session-2026-06-16-2254.md` (phiên First Principles → Mind Mapping → Morphological → Action Planning). Bối cảnh tham khảo: `src/hbc-sync` (cũ, sẽ deprecate) và báo cáo analyze `src/hbc-sync/.analysis/2026-06-16-1539/`.

**Quyết định lớn:** Đồng bộ được dựng lại thành **capability `impact` nhúng trong hbc-traceability** (không còn skill độc lập). Loại bỏ DAG tĩnh + hash manifest; chỉ ĐỌC matrix + task-status + phase-gate + git. 7 capability (CAP-1..7). 2 companion spec-authored (`architecture-diagrams.md`, `ref-skill-map.md`) + 1 adopted (`semantic-review-rubric.md`).

**Self-validate — Pass 1 (Coherence, Spec Law 1–6, 8):** PASS. Mỗi capability có cả intent và success (rule 1). Intent ở mức WHAT; CAP-5 nêu cơ chế `--invoked-by-sync` vì đó là ràng buộc chống-loop load-bearing, không phải prescription thừa (rule 2). Constraints đều bend design — "không sở hữu state", "không DAG", "suggest-not-decide" mỗi cái loại trừ cả một lớp phương án (rule 3). 6 non-goals tường minh (rule 4). Success signal là kịch bản REQ-010 cụ thể, viết test được (rule 5). CAP-1..7 mới, duy nhất (rule 6). Prose gọn (rule 8).

**Self-validate — Pass 2 (Preservation):** PASS. 8 nền + 3 tầng sự thật + quyết định B/C/A đều landed: Nền #1→Why; #2/#3→CAP-3 + non-goal (không lan ngược); #4→CAP-4/constraint; #5→constraint/CAP-1; #6/#8→CAP-2 + diagram; #7→CAP-4/ref-skill-map; B→CAP-7; C→CAP-6 + adopted rubric; A→CAP-1/constraint. Bảng ref→skill và hợp đồng freeze-check chuyển vào `ref-skill-map.md`. Sơ đồ chuyển vào `architecture-diagrams.md` (Spec Law: diagram luôn ở companion).

**Wrapper-only content (chủ động bỏ):** cấu trúc tiến trình của phiên brainstorming (tên 4 phase, tên kỹ thuật, lời dẫn facilitator) là process metadata, không phải nội dung hợp đồng — không đưa vào spec.

**Open questions còn mở (cần người quyết trước khi downstream tiêu thụ sâu):** baseline git cho đối chiếu; nơi đặt/override bảng ref→skill; fallback cho skill thiếu `update --invoked-by-sync`; kế hoạch deprecate `src/hbc-sync`.

## 2026-06-17 — Update: giải cả 4 Open Questions

User chốt cả 4 (không chỉnh). Đã đưa vào hợp đồng, mục Open Questions trong SPEC nay trống → gỡ bỏ.

- **OQ1 — baseline git:** working tree vs HEAD (thay đổi chưa commit), override `--since <ref>`. → CAP-1 + Constraint. *(Không dùng matrix-timestamp làm trigger — đó là tín hiệu audit-drift, khác mục đích.)*
- **OQ2 — nơi đặt bảng ref→skill:** `customize.toml` của hbc-traceability (`[workflow] ref_skill_map`). → Constraint + cập nhật `ref-skill-map.md`.
- **OQ3 — fallback skill thiếu update contract:** degrade graceful — interactive flag "cập nhật thủ công"; headless `blocked` reason `skill_no_update_contract` (thêm closed-set). → CAP-5 success + Assumption.
- **OQ4 — deprecate hbc-sync:** hai nhịp — đánh dấu DEPRECATED ngay (trỏ sang capability), gỡ hẳn (skill dir + `[SYNC]` menu + `module.yaml`) sau khi impact pass validation. → Non-goal.

Spec Law tái kiểm: rule 4 (non-goals) vẫn ≥1 và mạnh hơn; rule 3 (constraints bend design) — 2 constraint mới đều loại trừ phương án (baseline khác / map hard-code). PASS. Preservation giữ nguyên.

## 2026-06-17 — Update: áp review G1–G7 (workflow-builder review-as-contract)

Review SPEC như hợp đồng (không chạy skill-scanner vì target là spec, không phải skill) phát hiện 7 điểm; user duyệt áp tất cả.

- **G1 — CAP-2 phụ thuộc granularity design_ref:** thêm Assumption — over-flag được chấp nhận (an toàn) hơn bỏ sót khi matrix chỉ ghi mức thực thể.
- **G2 — git→REQ map mong manh cho code/design:** sửa Assumption — D-02 map qua cấu trúc; code/test/design map ngược qua matrix (`code_ref`/`test_ref`/`design_ref`→`req_id`).
- **G3 — thiếu dedup+sequencing xuyên REQ:** thêm Constraint — artifact dùng chung xử lý một lần; apply trước verify. CAP-4 phản ánh.
- **G4 — CAP-6 lớp "matrix tươi" là bằng chứng yếu:** hạ xuống đối chiếu phụ; 2 trụ chính = validator + LLM semantic review.
- **G5 — CAP-7 nửa coding-standard không có success:** thêm success — coding-standard flag code task chưa-đóng-băng để re-check (không string-grep rule).
- **G6 — impact khởi nguồn từ non-REQ artifact:** CAP-1 chuẩn hóa changed-set về REQ (reverse-map) trước phân tích.
- **G7 — đường thất bại reconcile:** CAP-6 — chưa-lan-đúng → đẩy lại SUGGEST, không clear, không rollback.
- **rule-2 (intent rò HOW):** trim intent CAP-1 và CAP-5; cơ chế giữ ở success/constraint.

**Self-validate Pass 1 (Coherence):** PASS. Cả 7 cap đủ intent+success (CAP-7 nay phủ cả glossary lẫn coding-standard, đóng hở rule 1/5). Intent đã rút HOW (rule 2). Constraint mới bend design (rule 3). Cap id CAP-1..7 giữ nguyên (rule 6).
**Self-validate Pass 2 (Preservation):** PASS. 7 mối lo của review đều landed; không mất source claim cũ.

## 2026-06-17 — Update: áp nhóm 🔴 từ edge-case-hunter

Edge-case-hunter (chạy trên SPEC như scope) liệt 17 biên chưa xử lý. User chốt áp nhóm 🔴 (sập hoặc sai im lặng):

- **#2 matrix chưa init:** CAP-1 success — dừng + gợi ý init, không phân tích.
- **#4 thay đổi không có trong matrix:** CAP-1 success — reverse-map rỗng → flag "chưa được trace", hỏi user.
- **#8 ba nguồn freeze bất đồng:** CAP-3 success — ưu tiên task status > phase-gate > matrix gate_status.
- **#12 owning-skill lỗi runtime:** CAP-5 success — branch-stop, giữ trạng thái, báo rõ, tiếp nhánh độc lập.
- **#14 vòng re-suggest vô hạn:** CAP-6 success — giới hạn (mặc định 2) → block + báo human.

Self-validate: PASS cả 2 pass; chỉ mở rộng success-criterion, không thêm/đổi cap id (CAP-1..7 giữ nguyên).

**Đã ghi nhận, CHƯA áp (trên record, không rơi im lặng) — chờ quyết sau:**
- 🟠 #1 REQ id không tồn tại · #3 changed-set rỗng (no-op) · #5 REQ bị xóa · #7 flood verify khi artifact dùng quá nhiều · #9 thiếu task-breakdown (fallback) · #10 design_ref không map được skill · #11 apply tập con · #13 thiếu validator cho loại artifact · #15 thuật ngữ glossary phổ biến gây flood.
- 🟡 #16 `--since` ref sai · #17 hai cascade song song (race/lock).

## 2026-06-17 — Update: fix all (12 finding biên còn lại)

User chốt "fix all". 12 finding biên (🟠/🟡) làm các dòng `success` phình run-on → tách thành companion mới **`edge-handling.md`** (kho duy nhất quy tắc biên, bảng theo từng CAP). Kernel rút gọn: mỗi CAP success giữ tiêu chí chính + một con trỏ "Biên: xem `edge-handling.md`". #17 (concurrency) thêm vào Constraints (là ràng buộc thiết kế, giữ inline). Companion thêm vào `companions:`.

Finding đã đóng: #1 (REQ id sai), #3 (no-op rỗng), #5 (REQ bị xóa), #7 (flood dùng chung), #9 (thiếu task-breakdown fallback), #10 (ref không map skill), #11 (apply tập con), #13 (thiếu validator), #15 (thuật ngữ phổ biến) + #6 (ref rỗng, gộp vào CAP-2), #17 (concurrency). Toàn bộ 17 finding edge-case nay đã xử lý (5 inline ở 🔴 trước + 12 ở companion/constraint).

**Self-validate Pass 1 (Coherence):** PASS. Lean prose phục hồi — success lines ngắn lại, chi tiết biên dồn vào companion (đúng Spec Law rule 8 + nguyên tắc "field phình sub-bullet → companion"). Cap id CAP-1..7 giữ nguyên. Constraint mới (concurrency) bend design (rule 3).
**Self-validate Pass 2 (Preservation):** PASS. Mọi quy tắc biên landed (kernel hoặc `edge-handling.md`); không mất claim. `edge-handling.md` đã có trong `companions:` để downstream đọc.
